# kubectl-rbac-flatten-plugin
used to flatten rbac roles, clusterroles, and their associated bindings - makes consolidation of rbac possible which improves the ability to manage multiple subjects and their access
